/* Today's rates — this is the ONLY file you need to edit daily.
   Every page reads these two numbers and fills in the rate badge. */
const RATES = {
  gold: 14538,   // ₹ per gram
  silver: 260    // ₹ per gram
};
